\title{AlgorithmTemplate}
\documentclass[12pt]{article}
\usepackage{fullpage}
\usepackage{times}
\usepackage{fancyhdr,graphicx,amsmath,amssymb}
\usepackage[ruled,vlined]{algorithm2e}
\include{pythonlisting}
\begin{document} 
\begin{algorithm}[H]
\SetAlgoLined

 \State $ plaintext \gets Alpha\:numeric\:password $\;
 \State $ Block Number \gets From \:1 \:to\: n $\;
 \While{cont.lower() == "y"}{
  Statement\;
  \eIf{1 \leq choice \:and\: choice <= n}{
   \State $ password \gets Strings $\;
   \eIf{plaintext == password}{
   Execute Blockchain blocks without Hacked blocks\;
   \State See Hacked Blocks \gets yes\: or\: no\;\newline
   \eIf{See Hacked Blocks == "yes"}{
   Fog.hello.printerr(plaintext,block\_number)\;
            \begin{lstlisting}
                def delay(block\_number) \newline
                     if(block == block\_number)\newline
                        break \;\newline
                        return hacked block information to fog\;\newline
                    else\newline
                        break\;\newline
                end class 
        \end{lstlisting}
   }{
   Break\;
   }
   }{
        \begin{lstlisting}
                class block()\newline
                        return block;\newline
                    end class\newline
                    class blockchain.main()\newline
                     if block==1:\newline
                         for n in range( block ):\newline
                             blockchain.mine(Block("Block " + str(n+1)))\newline
                         for n in range(n-1):\newline
                             blockchain.mine(Block("Block " + str(n+ block +1)))\newline
                end class
        \end{lstlisting}
  }

   }{
   Invalid Input\;\newline
   \State Cont \gets yes\: or\: no \;\newline
   \eIf{Cont == "yes"}{
   continue\;
   }
   {
   break\;
   }
  }
 }
 \caption{Edge Hacked Algorithm}
\end{algorithm}


\end{document}